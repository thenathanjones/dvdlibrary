%html
  %head
    %title= content_for?(:title) ? "DVD Library - #{yield :title}" : "DVD Library"
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = csrf_meta_tag
  %body
    #header-container.row
      = render :partial => "layouts/header"
    
    #body-container.container
      = yield